fields locale : String, currency : String, price : int, quality : double
vars expectedLocale : String, expectedCurrency : String, minPrice : int, maxPrice : int, qualityCutoff : double
query
    locale == expectedLocale and
    currency == expectedCurrency and
    price >= minPrice and
    price <= maxPrice and
    quality > qualityCutoff
