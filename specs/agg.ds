Aggr:

    state l : Bag<Int>

    op add(i : l.Handle)
        l.add(i)

    query all()
        l

    query totalSum()
        sum [ i.val | i <- l ]

    query countGt10()
        sum [ 1 | x <- l, x.val > 10 ]

//    query minimum()
//        min l

//    query maximum()
//        max l
