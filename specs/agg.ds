Aggr:

    state l : Bag<Int>

    op add(i : Int)
        l.add(i)

    query elems()
        l

    query totalSum()
        sum [ i.val | i <- l ]

    query countGt10()
        sum [ 1 | x <- l, x.val > 10 ]

//    query minimum()
//        min l

//    query maximum()
//        max l
