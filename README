Data Structure Synthesis

This file documents how to use the data structure synthesis tool.

Dependencies:
    Python 2.7
    Z3 Python bindings

To check for the necessary dependencies, you can run:
    $ ./check.sh

To get help:
    $ python src/main.py --help

To run the tool:
    $ python src/main.py FILE

Sample input files live in the "examples" folder.

The synthesizer may run for a very long time in some cases. To stop it, press
Ctrl+C and the tool will stop and output the best plan found so far.
Alternatively, you can pass a timeout value to the tool.

Detailed documentation lives in the "docs" folder. To build the PDFs:
    $ make -C docs
